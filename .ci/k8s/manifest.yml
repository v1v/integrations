apiVersion: v1
kind: Pod
spec:
  containers:
  - name: ubuntu
    image: ubuntu:20.04
    command:
    - sleep
    args:
    - infinity
  - name: golang
    image: golang:1.17.6
    command:
    - sleep
    args:
    - infinity
  - name: docker
    image: docker:20.10.12-dind
    securityContext:
      privileged: true
    env:
      - name: DOCKER_TLS_CERTDIR
        value: ""
